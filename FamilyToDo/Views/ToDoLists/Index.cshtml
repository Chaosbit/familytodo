@model IEnumerable<FamilyToDo.Models.ToDoList>

@{
    Layout = "/Views/Shared/_Layout.cshtml";
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th><a asp-action="Create" title="New"><span class="glyphicon glyphicon-plus" aria-hidden="true"></a></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <a asp-action="Details" asp-route-id="@item.ID" title="Show todo items ...">@Html.DisplayFor(modelItem => item.Name)</a>
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.ID" title="edit"><span class="glyphicon glyphicon-pencil" aria-hidden="true"></a>
                        <a asp-action="Delete" asp-route-id="@item.ID" title="delete"><span class="glyphicon glyphicon-remove aria-hidden="true"></a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
